---
description: Captura de baja fricción de una idea/nota al inbox del segundo cerebro
---

Capturá lo siguiente en `inbox/` del vault (esta carpeta), sin clasificar ni decidir destino final — eso lo hace `/ordenar-inbox` después.

Contenido a capturar: $ARGUMENTS

Delegá esto a un sub-agente `general-purpose` con `model: haiku` (tarea mecánica, sin criterio):
1. Crear un archivo markdown nuevo en `inbox/` con frontmatter mínimo (`name`, `description`, fecha) y el contenido tal cual, sin reformular de más.
2. Nombre de archivo: slug corto derivado del contenido, con extensión `.md`.
3. No clasificar, no aplicar plantilla, no enlazar `[[...]]` — solo capturar.

No preguntes nada, ejecutá directo.
